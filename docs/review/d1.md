# Review: product/d1 — porcelain: d1

- **Implementer:** codex gpt-5.6-terra high (wave 2)
- **Coordinator approval:** CO.9dc4 (gpt-5.6-sol) at d334b36, after 1
  rework round (enum normalization, jurisdiction/location exclusivity,
  whitespace SQL rejection)
- **Final review:** maintainer (Claude Fable)
- **Verdict:** squash-merged to main as c21b690, 2026-08-11

## What was checked

- **Spec conformance:** `/accounts/{account_id}/d1/database` CRUD,
  `/{database_id}/query`, `/{database_id}/export` (polling with bookmark)
  all match the pinned spec.
- **Gate:** full `make check` (incl. fmt-check) green at d334b36 and on
  main post-merge.
- **Scope:** d1.go + d1_test.go + root.go wiring only.

## Not in scope (available via cf api d1)

`/raw`, `/import`, `/time_travel/*` — time-travel restore is the most
interesting fast-follow (`cf d1 restore --to <bookmark|timestamp>`).
